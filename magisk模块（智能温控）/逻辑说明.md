# tempctrl 智能温控守护程序 — 设计说明

对应实现：[tempctrl.c](tempctrl.c)
配置文件：[magisk模块框架/profile.conf](magisk模块框架/profile.conf)
本文件只说明运行逻辑，不追求代码级精细。

---

## 整体架构

```
 ┌──────────────────┐               ┌──────────────────┐
 │ LSPosed 模块     │   am broadcast │ tempctrl 守护进程 │
 │ (MainHook.java)  │ ←────────────│ (本 C 程序)      │
 │                  │   mode/temp/  │                   │
 │ 收到广播 → 调用   │   windOC/    │ pgrep → 检测 App  │
 │ WaspWingManager  │   coldOC/    │ 存活状态         │
 │ .setRunMode()    │   windLevel   │ 每 5s main_loop() │
 │      ↓           │               │                   │
 │ BLE → 散热器     │               │ 热重载 profile.   │
 └──────────────────┘               │ conf 配置         │
                                    └──────────────────┘
```

**通信方式变更（v2.0）**：~~FIFO 写入~~ → 已废弃，改为 `pgrep` 检测 App 进程存活。
tempctrl 不再通过 FIFO 接收唤醒/休眠信号，而是每 5 秒检查进程是否存在。

---

## 配置文件系统 (profile.conf)

tempctrl 支持运行时配置文件，所有关键阈值和参数均可外部修改，无需重新编译。

### 配置路径自动检测

1. **`--config` 参数**（最高优先级）：
   ```shell
   tempctrl --config /自定义路径/profile.conf
   ```
2. **自动检测**：通过 `/proc/self/exe` 获取自身路径，在同目录下查找
3. **默认值**：全部使用代码内默认值

### CONFIG_ENABLED 开关

配置文件第一行必须是 `CONFIG_ENABLED=1` 才能加载，否则所有参数使用代码默认值。
这对调试很有用——**设置 `CONFIG_ENABLED=0` 可快速排除配置文件问题**。

### ⚠️ 常见坑

| 坑 | 说明 |
|----|------|
| **Termux 编译不可用** | Termux 的 `clang -static` 链接的是 Termux 的 libc，非 Android libc。编译出的二进制在真机上 PT_TLS 对齐错误，必须用 NDK 编译。**开发阶段在 Termux 编出来可以看语法错误，但不要拿去真机跑** |
| **`catch(Throwable)`** | Xposed 的 `findAndHookMethod` 找不到方法会抛 `NoSuchMethodError`（继承自 `Error`），`catch(Exception)` 接不住，必须 `catch(Throwable)`。否则一个钩子失败会导致后面所有钩子注册不上 |
| **模块未响应** | 如果 LSPosed 日志看到"收到广播"但散热器没反应，检查 WaspWingManager 实例是否捕获成功（日志应有"已捕获 WaspWingManager 实例"）。如果看不到，检查 B6ExperimentalActivity 是否加载成功 |
| **status 文件无内容** | 模块的 `startPeriodicStatusWrite()` 在 Application.onCreate 中启动，如果前面的钩子注册异常（如 NoSuchMethodError 没接住），onCreate 钩子不会被执行 |
| **`--config` 路径** | service.sh 使用 `--config "$MODDIR/profile.conf"`直接从模块目录读取，不再复制到 `/data/local/tmp/` |

### CONFIG_ENABLED 开关

配置文件首行必须是 `CONFIG_ENABLED=1` 才会加载全部参数。
`=0` 时跳过所有配置，使用代码默认值。可用来快速排除配置问题。

### 配置格式

```
# 注释以 # 开头
KEY=VALUE

# 例：
BATT_BASELINE=350
BATT_ZONE_1=7
```

不认识的 key 静默跳过；每个值有范围保护（`clamp`），非法值不会导致进程崩溃。

### 热重载

每轮 `main_loop()`（5 秒间隔）开头 `stat()` 检查配置文件的修改时间（mtime），变了就自动重读所有参数并记日志。**修改配置无需重启 daemon**。

### 可配置参数一览

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `CONFIG_ENABLED` | 1 | 配置文件总开关（0=全部默认） |
| `BATT_BASELINE` | 350 | 电池基准温度 (0.1°C) |
| `BATT_ZONE_1` | 7 | 第一区间阈值 (0.1°C)，死区 |
| `BATT_ZONE_2` | 20 | 第二区间阈值 (0.1°C) |
| `OVERRIDE_MAX` | 8 | 趋势豁免最大连续次数 |
| `PEAK_DAMP_THRESHOLD` | 3 | 2°C 外严格豁免阈值 (0.1°C) |
| `PEAK_DAMP_INNER_THRESHOLD` | 5 | 2°C 内峰值反补触发阈值 (0.1°C) |
| `PEAK_DAMP_INNER_ADJUST` | 2 | 2°C 内峰值反补档位数 |
| `PEAK_DAMP_OUTER_THRESHOLD` | 3 | 2°C 外峰值反补触发阈值 (0.1°C) |
| `PEAK_DAMP_OUTER_ADJUST` | 1 | 2°C 外峰值反补档位数 |
| `CPU_EMERG_1/2/3` | 650/750/850 | CPU 紧急触发温度 (0.1°C) |
| `CPU_RECOVER_0/1/2` | 550/650/750 | CPU 紧急恢复温度 (0.1°C) |
| `EMERG_FORCED_1/2/3` | 6/8/10 | 各紧急等级强制最低档位 |
| `CPU_FILTER_ALPHA` | 20 | CPU 滤波系数 (百分比) |
| `CPU_ZONE_MIN` | 0 | CPU 温度扫描起始 zone（K60 推荐 30） |
| `CPU_ZONE_MAX` | 99 | CPU 温度扫描结束 zone（K60 推荐 40） |
| `DISCONNECT_RESET_SEC` | 60 | BLE 断联超时重置秒数 |
| `INIT_TEMP_OFFSET` | 350 | 初始档位基准温度 (0.1°C) |
| `INIT_DIVISOR` | 10 | 初始档位除数（每多少°C一档） |
| `STATUS_TIMEOUT` | 16 | 状态文件心跳超时判死秒数 |
| `LOG_FILE` | /cache/<二进制名>.log | 日志文件路径 |
| `LOG_MAX_KB` | 10 | 日志文件大小上限 (KB)，0=关闭日志 |

---

## 温度读取

- **电池温度**：`/sys/class/power_supply/battery/temp`
  - 原生 0.1°C 精度，如 350 = 35.0°C

- **CPU 温度**：`/sys/class/thermal/thermal_zone[N]/temp`
  - 原始值 m°C，÷100 转 0.1°C，如 75321 → 753 (=75.3°C)
  - 首次调用时在 `CPU_ZONE_MIN~CPU_ZONE_MAX` 范围内扫描，记录能读到有效值的 zone
  - 后续只扫描已记录的 zone，取最高值
  - 一阶低通滤波平滑（系数由 `CPU_FILTER_ALPHA` 配置，默认 α=0.25）

---

## 档位系统

档位 1~12，使用查表法（非公式计算）：

| 档位 | 模式 | 目标温 | 风扇RPM | 制冷片 | 说明 |
|-----|------|-------|--------|-------|------|
| 12 | 固定功率 | — | 6000 | 190 | 制冷峰值 |
| 11 | 智能温控 | 12°C | 5800 | auto | 紧急3-min |
| 10 | 智能温控 | 13°C | 5500 | auto | |
| 9 | 智能温控 | 14°C | 5100 | auto | 紧急2-min |
| 8 | 智能温控 | 15°C | 4600 | auto | |
| 7 | 智能温控 | 16°C | 4000 | auto | 紧急1-min |
| 6 | 智能温控 | 18°C | 3400 | auto | |
| 5 | 固定功率 | — | 2900 | 80 | 低功耗1 |
| 4 | 固定功率 | — | 2500 | 60 | 低功耗2 |
| 3 | 固定功率 | — | 2200 | 40 | 低功耗3 |
| 2 | 固定功率 | — | 2000 | 20 | 伪待机 |
| 1 | 固定功率 | — | 2000 | 10 | 超低待机 |

控制指令通过 `am broadcast` 发送：

```shell
adb shell am broadcast --user 0 \
  -a com.flydigi.SET_TEMPERATURE \
  --ei mode <0/1> \
  --ei temperature <目标温度> \
  --ei windOC <固定转速> \
  --ei coldOC <制冷强度> \
  --ei windLevel <智能转速上限>
```

- **智能模式 (mode=0)**：设目标温度和风扇上限，散热器自行调节制冷
- **固定功率 (mode=1)**：直接指定风扇转速和制冷片强度

---

## 主循环逻辑

`main_loop()` 每 **5 秒**执行一次：

0. **检查配置更新** → `stat()` 检测 `profile.conf` 修改时间，变了就热重载
1. **紧急干预** → 读 CPU 温度 → 低通滤波 → 判定紧急等级 (0~3)，设 `forced_min_level`
2. **电池控制** → 读电池温度 → 计算温度变化量 + 常规档位调整量
   - 温度读数不变则跳过
   - **合并逻辑**（趋势豁免 + 峰值反补合为一体）：
     - 变动 ≤0.3°C 且趋势反向 → **趋势豁免**（跳过本次常规调整），首次触发记日志
     - 变动 >0.3°C → **峰值反补**（直接改档位，跳过常规调整）：
       - 2°C 内 0.3~0.5°C → 反补 1 档
       - 2°C 内 >0.5°C → 反补 PEAK_DAMP_INNER_ADJUST 档（默认 2）
       - 2°C 外 >0.3°C → 反补 PEAK_DAMP_OUTER_ADJUST 档（默认 1）
   - 未被豁免/反补时执行常规升降档（偏差三区策略）
3. **合最终档位** = `max(battery_fan_level, forced_min_level)`
4. **下发**控制参数（去重）
5. **同步** `battery_fan_level = final_level`（电池以下次以上次实际档位为基础）
6. **退出紧急限制**：如果刚退出紧急等级且档位过高 → 硬 cap 到 `EMERG_FORCED_X`
   - 大电流充电（`current_now < -2000mA`）时跳过此限制
7. **pgrep 检测** App 进程存活 → 失联则等待复活后强制重发

---

## 进程检测机制

使用 **双重检测**：pgrep 进程存在检查 + 状态文件 mtime 心跳检查。只要有一个出问题就算断联。

```
 启动 → 创建 status 文件（0666，所有用户可写）
      → 等待 App 进程出现（pgrep 通过即可进入）
 
 工作模式中，每 5 秒：
   main_loop() → 决策 → 下发
   
   1. 读 status 文件内容 → BLE=0/1（模块上报的 BLE 连接状态）
   
   2. 双重存活检测：
      ├─ pgrep -f com.flydigi.waspwing.experimental  → 进程存在?
      └─ stat(status 文件).mtime 距现在 ≤ 16秒？     → 模块在写心跳?
      
      ├─ 两者都 OK → 存活
      └─ 任一失败  → App 死了 → 记录失联时间
                          ↓
                      等待复活子循环（每 5 秒检测）
                          ↓
                      App 重新出现 → 强制重发当前档位
                          ↓
                      检查断联时间
                        ├─ ≥ DISCONNECT_RESET_SEC → reset_state()
                        └─ < DISCONNECT_RESET_SEC → 正常恢复

 模块侧（LSPosed）：
   ────────────────────────────────
   onDeviceConnected / disconnect 钩子 → 更新 bleConnected
   每 5 秒线程：FileOutputStream 写 "BLE=0/1\n" 到 status 文件
   （直接 Java 文件 I/O，不走 su，无 KSU 命名空间问题）
```

### 文件详情

| 项目 | 说明 |
|------|------|
| 路径 | `/data/local/tmp/tempctrl.status` |
| 创建者 | tempctrl 启动时 `fopen("a")` + `chmod 0666` |
| 写入者 | LSPosed 模块每 5 秒覆写 `BLE=0\n` 或 `BLE=1\n` |
| 读取者 | tempctrl `main_loop` 后 `read_status_ble()` |
| 心跳超时 | 16 秒（STATUS_TIMEOUT，写死后约 3 轮检测判死） |

### 双检测互补

| 场景 | pgrep | 文件 mtime | 结果 |
|------|-------|-----------|------|
| App 正常运行，BLE 已连 | ✅ | ✅（新鲜 BLE=1） | 存活 |
| App 正常运行，BLE 断联 | ✅ | ✅（新鲜 BLE=0） | 存活（但不控温） |
| App 被 kill | ❌ | ❌（不再更新） | 断联 |
| App 卡死/ANR | ✅（进程在） | ❌（无写入） | 断联 |
| 文件被意外删除 | ✅ | ❌（stat 失败） | 断联（daemon 会重建） |

---

## 电池控制细节

- **基准温度**：35.0°C（`BATT_BASELINE=350`，可配置）
- **三级策略**（diff = 当前温度 - 基准温度）：
  - `|diff| ≤ BATT_ZONE_1` → 不动，属于正常范围
  - `BATT_ZONE_1 < |diff| ≤ BATT_ZONE_2` → 升/降 1 档
  - `|diff| > BATT_ZONE_2` → 升/降 2 档

- **档位继承同步**：
  - 每次 `main_loop` 末尾 `battery_fan_level = final_level`
  - 电池控制始终从上次实际下发的档位开始调整
  - 紧急退出后不会暴跌——电池继承了紧急期间的档位
  - 退出紧急限制在同步之后执行：如果档位过高则硬 cap

- **温度趋势豁免（融合峰值时间检测）**：
  - **基准 2°C 以内**：趋势反向即豁免（降温中要升档 → 跳过，升温中要降档 → 跳过）
  - **超过 2°C**：仅当单次变动 >PEAK_DAMP_THRESHOLD（默认 0.3°C）才豁免，温度偏离较大时更积极响应
  - 最多连续豁免 `OVERRIDE_MAX` 次（默认 8 次 ≈ 40 秒），之后强制执行
  - 最高/最低档位时不记日志（避免刷屏）
  - 防止温度小幅波动导致反复升降档

- **峰值过冲抑制**：
  - **基准 2°C 内**：
    - **0.3~0.5°C**（3~5 单位）→ 反补 **1** 档
    - **>0.5°C**（>5 单位）→ 反补 `PEAK_DAMP_INNER_ADJUST` 档（默认 **2**）
  - **基准 2°C 外**：
    - **>0.3°C**（>3 单位）→ 反补 `PEAK_DAMP_OUTER_ADJUST` 档（默认 **1**）
  - 激励方向：急升→加压档，急降→减压档
  - 此逻辑始终触发且触发后本周期常规 delta 不执行

- **防重复**：
  - 记录上次调整时的电池温度（`prev_batt_temp`）
  - 如果当前温度与该值相同，跳过本次升降档
  - 死区内也更新该记录，避免反复进入计算

- **冷却期已移除**：已删除所有冷却期逻辑（包括 CPU 紧急的冷却）。

---

## CPU 紧急干预细节

- 使用一阶低通滤波：`weighted = raw × CPU_FILTER_ALPHA% + old × (100-CPU_FILTER_ALPHA)%`
- 首次运行直接赋值，不做滤波
- 滤波系数默认 20 (α=0.20)，可通过 `CPU_FILTER_ALPHA` 配置（越大越跟原始值）

**紧急等级**（阈值均可通过 profile.conf 调整）：

| 等级 | 触发条件（默认） | 强制最低档位（默认） |
|------|----------------|-------------------|
| 0 | 正常 | 无 |
| 1 | CPU > 65°C (`CPU_EMERG_1`) | 6（智能 16°C/4000RPM） |
| 2 | CPU > 75°C (`CPU_EMERG_2`) | 8（智能 15°C/4600RPM） |
| 3 | CPU > 85°C (`CPU_EMERG_3`) | 10（满功率 6000RPM/190） |

**退出紧急时**限制电池档位上限（在同步之后执行，保证生效）：
- 仅在电池温度低于升档阈值（`BATT_BASELINE + BATT_ZONE_1`=35.7°C）时执行
- 如果电池温度仍高于升档阈值，说明散热需求仍在，不降档（防止 CPU 温度反弹）
- 满足条件时：
  - 退出等级 3 → `battery_fan_level ≤ EMERG_FORCED_3`
  - 退出等级 2 → `battery_fan_level ≤ EMERG_FORCED_2`
  - 退出等级 1 → `battery_fan_level ≤ EMERG_FORCED_1`

---

## 日志系统

- **默认日志路径**：`/cache/<二进制名>.log`（自动根据二进制文件名生成）
  - 例：`tempctrl` → `/cache/tempctrl.log`
- 可通过 `profile.conf` 的 `LOG_FILE` 覆盖
- **日志滚动**：超 `LOG_MAX_KB`（默认 10KB）后自动删除最早日志行
- **日志关闭**：`LOG_MAX_KB=0` 完全关闭日志写入
- **格式**：`[日 时:分:秒] 日志内容`
  - 例：`[15 14:30:22] 档位3 +2`
- **主要日志含义**：

| 日志 | 含义 |
|------|------|
| `档位3 +2` | 电池常规升降档，齿轮 3 → +2 → 5 |
| `过冲0.4 挡位8 +1` | 峰值过冲抑制，温度骤变 0.4°C，齿轮 8 → +1 → 9 |
| `趋势豁免 7` | 温度趋势反向，本周期跳过升降档（停留在 7 档） |
| `紧急 1 档+1 cpu=69.0` | CPU 紧急等级变化，1 级 → 2 级 |
| `App 消失` | pgrep 或 status 文件检测到进程失联 |
| `App 恢复(断30s)` | 进程恢复，断联 <60 秒，继续运行 |
| `App 恢复(断118s≥60s) 重置状态` | 进程恢复，断联 ≥60 秒，全部状态归零 |

---

## Magisk 模块框架

```
magisk模块框架/
├── module.prop       ← 模块元信息（id/name/author/version）
├── service.sh        ← 开机自动拉起 tempctrl（等待系统启动完成）
├── customize.sh      ← 刷入时设权限（二进制 0755，配置 0644）
├── profile.conf      ← 所有可调参数（带中文注释）
└── tempctrl          ← 编译好的二进制（仅限 CI/NDK 编译，Termux 不可用）
```

---

## 启动流程

1. 设定默认日志路径（根据二进制文件名）
2. 加载配置（`--config` 参数 > 自动检测 > 全部默认值）
3. 读电池温度计算初始档位
4. 进入工作模式后每 5 秒执行 `main_loop()`
   - `main_loop` 中通过 `pgrep` 检测 App 进程存活
   - App 未运行时等待复活，复活后强制重发当前档位

---

## 待测试项

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 双重进程检测 | 🟡 中 | pgrep + status 文件 mtime 16 秒超时，断写心跳后能否正确判死并恢复 |
| Status 文件 BLE 状态上报 | 🟡 中 | 模块每 5 秒写 BLE=0/1，daemon 正确读取 |
| config 热重载 | 🟡 中 | 修改 `profile.conf` 后参数即时生效 |
| 峰值过冲抑制 | 🟡 中 | 0.3~0.5°C 反补 1 档、>0.5°C 反补 2 档是否合理 |
| 0.3~0.5°C 新增档次 | 🟡 中 | 新增的中间档位（1档反补）是否正确触发 |
| 退出紧急降档验证 | 🟡 中 | 电池高于升阈时跳过 cap、大电流充电时跳过 cap |
| 日志滚动 | 🟢 低 | 超 LOG_MAX_KB 后最早行被删除 |
| CONFIG_ENABLED=0 测试 | 🟢 低 | 关闭配置加载后全部使用默认值 |

---

## 注意事项

- **CPU 温度扫描范围** 通过 `CPU_ZONE_MIN/MAX` 配置（默认 0~99 全扫），K60 机型推荐 30~40 
- **温度传感器路径** `thermal_zone[N]` 是 Linux 通用接口，不同 SoC 的 zone 编号不同。全自动扫描无需手动配置，但如果扫描范围过大（0~99）首次可能稍慢（约 1 秒），可通过 `CPU_ZONE_MIN/MAX` 缩小范围
- **去重逻辑**：`apply_level()` 缓存上次参数，连续相同不下发，但 App 复活时强制清除缓存重发
- **`patch_tls.py`**：NDK 静态编译后需要执行，修复 PT_TLS 对齐；无 TLS 段时返回非零，需要 `|| echo "skipped"` 容忍
- **`#include <stdarg.h>`**：`write_log()` 用了 `va_list`，NDK clang 下不是隐式包含的
- **FIFO 已废弃**：原 FIFO 通信（`/data/local/tmp/b6x_fifo`）已删除，改用 `pgrep` 检测 App 进程
- **KernelSU noexec**：KernelSU 模块目录挂载了 `noexec`，`service.sh` 会将二进制复制到 `/data/local/tmp/` 执行
